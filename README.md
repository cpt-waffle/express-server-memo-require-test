## A memo test of require


```
npm i
```

make db called `w05d03`
make user for psql called `labber` with pass `labber` (or change credentials in `connection.js`)
make sure psql runs on `5432` 


start server 